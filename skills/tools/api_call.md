# api_call — HTTP API Client

Make HTTP requests to external APIs.

**Usage:** `api_call(method="GET", url="https://...", headers={...}, body="...")`

**When to use:**
- Calling REST APIs, fetching data from the web
- Integrating with external services

**For simple HTTP operations**, `bash` with curl/wget may be more familiar.
