# clear_log_file — Truncate Agent Logs

Truncates the agent's llm.log and sessrecap.log files with a reset marker.

**Usage:** `clear_log_file()` — no parameters

**When to use:**
- Resetting agent logs for a fresh start
- Clearing accumulated log data
