# sshc — SSH Client

Execute commands on remote servers via SSH.

**Usage:** `sshc(host="...", command="...")`

**When to use:**
- Managing remote servers
- Checking remote system status
- Deploying to remote hosts

**Ensure you have proper credentials configured.**
